// Nerdamer Method.
    // let matrix = `[${aVal}, ${bVal}, ${cVal}, ${dVal}-x, 0, 0]`
    //          + `, [0, ${aVal}, ${bVal}, ${cVal}, ${dVal}-x, 0]`
    //          + `, [0, 0, ${aVal}, ${bVal}, ${cVal}, ${dVal}-x]`
    //          + `, [${pVal}, ${qVal}, ${rVal}, ${sVal}-y, 0, 0]`
    //          + `, [0, ${pVal}, ${qVal}, ${rVal}, ${sVal}-y, 0]`
    //          + `, [0, 0, ${pVal}, ${qVal}, ${rVal}, ${sVal}-y]`;
    // nerdamer.setVar('M', `matrix(${matrix})`);
    // resultant = nerdamer('determinant(M)');
    // console.log(resultant.toString());
    // console.log("-");
    // console.log("-");
    // console.log("-");
    // console.log(resultant.text('decimals'));
    // console.log("-");
    // console.log("-");
    // console.log("-");
    // console.log(resultant.toTeX());